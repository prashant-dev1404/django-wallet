# payouts/apps.py
#
# class PayoutsConfig(AppConfig):
#     default_auto_field = "django.db.models.BigAutoField"
#     name = "payouts"
#
#     def ready(self):
#         # Import workers here only if they need signal hookups.
#         # Django-Q discovers tasks automatically when called by name; no
#         # registration needed here.
#         pass
